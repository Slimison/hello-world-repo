name: My first example

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest  
    steps:
    - name: Welcome message
      run: echo "My first github action job"

    - name: list files
      run: ls -la

    - name: read file
      run: cat README.md   